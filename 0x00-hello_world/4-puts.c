#include <stdio.h>

/**
 * main -prints the phrase with the proper grammar
 * Return :0 if executed properly ,non-zero
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0)
}
